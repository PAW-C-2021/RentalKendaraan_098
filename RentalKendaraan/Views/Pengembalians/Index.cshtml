@model IEnumerable<RentalKendaraan.Models.Pengembalian>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TglPengebalian)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPeminjaman)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Denda)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdKondisiNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPengembalianNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TglPengebalian)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPeminjaman)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Denda)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdKondisiNavigation.IdPeminjaman)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPengembalianNavigation.IdKondisi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPengembalian">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPengembalian">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPengembalian">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
